@{
    ViewBag.Title = "Missions";
}

<h2>Missions</h2>

@using (Html.BeginForm("Missions", "Home", FormMethod.Post))
{
    <fieldset>
        <h4>Select a Mission</h4>
        @Html.DropDownList("Mission", null, new { id="DropDownList"})
        <input class="MissionButton" type="submit" value="Learn more!" />
    </fieldset>
}